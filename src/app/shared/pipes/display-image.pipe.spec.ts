import { DisplayImagePipe } from './display-image.pipe';

describe('DisplayImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayImagePipe();
    expect(pipe).toBeTruthy();
  });
});
